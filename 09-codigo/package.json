{
  "name": "authority-engine",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/tests/*.test.js",
    "check": "npm run build && npm test",
    "start": "node dist/start.js",
    "dev": "npm run build && node dist/start.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.23.1",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
